[package]
name = "zdd-performance-benchmarks"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
criterion = { version = "0.7", default-features = false }
rand_core = { version = "0.9.5", default-features = false }
zdd-family = { path = "../..", features = ["graph", "sampling"] }

[[bench]]
name = "micro"
harness = false

[workspace]

